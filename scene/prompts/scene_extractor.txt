### Extraction Rules:
1. **Scene Identification**: Generate a descriptive 'name' for the overall scene.
2. **Action Decomposition**: Break the script into a sequential list of 'actions' (storyboard panels).
3. **Action Details**: For each action, provide:
   - 'order': The sequential index (starting from 0).
   - 'name': A short, descriptive title for the action.
   - 'prompt': A detailed visual description optimized for high-quality image generation (nanobanana).
   - 'prompt_comic':  optional. A visual description optimized for a comic book panel style (mentioning line work, ink, or stylized colors).
   - 'prompt_video': optional.  A motion-focused description (e.g., camera movement like 'panning', 'dolly-in', or specific character movements).
   - 'prompt_voice': optional. The dialogue or narration text occurring during this specific action.
 - 'voice': the voice name 

4. **Entity Extraction (Props, Backgrounds, Characters)**:
   - **Background**: For every action, identify the specific environment (Location). Provide a 'name' and a 'prompt' describing the setting's visual appearance (otimise the prompt for nanobanana)
   - **Props**: List all physical objects ('elements') involved in the action. For each, provide a 'name' and a visual 'prompt'.  (optimise the prompt for nanobanana)
   - **Characters**: List all characters appearing in the action. In the action object, use the 'characters' field to list their names. For each character, provide a 'name' and a 'prompt' describing their physical appearance, clothing, and current expression. (optimise the prompt for nanobanana)

 - **Voices**: 
 List all voices required appearing in the action. In the action object, use the 'voices' field to list their names. For each voice, provide a 
 {'name':"voice name (if linked to a character it is the same name of the character)" 
'google_voice': 'choose the name from the provided list of google voices',
 'prompt':  'markdown format with following voice prompt 
    #Style: <style description optimised  gemini tts model > <example Deeply academic, cinematic, and filled with quiet awe and gravity. >
    #Pace:  <pace description optimised  gemini tts model > <example Highly measured, deliberate, and slow with pronounced, dramatic pauses. 
    #Accent: <pace description optimised  Elite British Received Pronunciation (RP) with clear, crisp diction.>'
}


### Consistency Requirements:
- **Strict Naming**: Use the EXACT same 'name' for the same Background, Character, or Prop if it appears across multiple actions. 
- **Reuse Context**: If a list of existing entities is provided in the context, you MUST reuse their exact 'name' for matching entities in the story, do not output a prompt for them, only work on the appearance. Do not create duplicates.
- **Visual Persistence**: Ensure the 'prompt' for a specific named element (e.g., a specific character's appearance) is consistent every time that element is referenced.

### Output:
Return only valid JSON that strictly adheres to the requested schema